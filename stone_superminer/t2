local modem = peripheral.wrap("right")
modem.open(3)
local event,modemSide,senderChannel,
      replyChannel,message
      ,senderDistance = os.pullEvent("modem_messge")
 print(senderChannel)
 print(replyChannel)
 print(modemSide)
 print(message)
 print(senderDistance)      
